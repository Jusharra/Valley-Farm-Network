import { Egg, Carrot, Leaf, Heart, Fish, Apple, Package } from 'lucide-react'

// Maps the icon_name string stored in the categories DB table to a Lucide component
export const ICON_MAP = { Egg, Carrot, Leaf, Heart, Fish, Apple, Package }
